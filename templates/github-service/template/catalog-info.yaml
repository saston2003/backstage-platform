apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: Service ${{ values.name }}
spec:
  type: service
  lifecycle: experimental
  owner: ${{ values.owner }}